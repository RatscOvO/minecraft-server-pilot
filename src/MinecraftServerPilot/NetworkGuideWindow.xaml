<Window x:Class="MinecraftServerPilot.NetworkGuideWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="朋友联机与网络诊断" Width="760" Height="590"
        MinWidth="640" MinHeight="480" WindowStartupLocation="CenterOwner">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackPanel>
            <TextBlock Text="朋友联机与网络诊断" FontSize="22" FontWeight="Bold" />
            <TextBlock Text="先从局域网验证，再处理公网映射或可信隧道。"
                       Foreground="{StaticResource MutedBrush}" Margin="0,5,0,0" />
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,18,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Border Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1" CornerRadius="9" Padding="14">
                <StackPanel>
                    <TextBlock Text="局域网连接地址" Foreground="{StaticResource MutedBrush}" FontSize="11" />
                    <TextBlock x:Name="AddressText" FontSize="16" FontWeight="SemiBold" Margin="0,5,0,0"
                               TextWrapping="Wrap" />
                </StackPanel>
            </Border>
            <Border Grid.Column="2" Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1" CornerRadius="9" Padding="14">
                <StackPanel>
                    <TextBlock Text="本机端口状态" Foreground="{StaticResource MutedBrush}" FontSize="11" />
                    <TextBlock x:Name="PortStatusText" FontSize="16" FontWeight="SemiBold" Margin="0,5,0,0" />
                </StackPanel>
            </Border>
        </Grid>

        <Border Grid.Row="2" Background="#2B2212" BorderBrush="#6D5423" BorderThickness="1"
                CornerRadius="8" Padding="12" Margin="0,14,0,0">
            <TextBlock x:Name="SafetyText" Foreground="#F2D28B" TextWrapping="Wrap" />
        </Border>

        <TextBox x:Name="GuideTextBox" Grid.Row="3" IsReadOnly="True" AcceptsReturn="True"
                 TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Margin="0,14,0,0"
                 Padding="14" />

        <Grid Grid.Row="4" Margin="0,14,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="8" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="8" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="8" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="公网连通性还取决于路由器、运营商 CGNAT 或隧道。"
                       Foreground="{StaticResource MutedBrush}" VerticalAlignment="Center"
                       TextWrapping="Wrap" Margin="0,0,12,0" />
            <Button Grid.Column="1" Content="复制地址" Click="CopyAddressButton_OnClick" />
            <Button Grid.Column="3" Content="刷新检测" Click="RefreshButton_OnClick" />
            <Button Grid.Column="5" Content="创建防火墙规则" Click="FirewallButton_OnClick" />
            <Button Grid.Column="7" Content="关闭" Click="CloseButton_OnClick"
                    Background="#238636" BorderBrush="#2EA043" />
        </Grid>
    </Grid>
</Window>

# 原始需求完成度审计

审计依据：根目录 `要求.txt`、当前源码、自动测试、真实 1.7.10 安装记录和发布产物。  
状态含义：**已验证**表示存在与需求范围相符的直接证据；**部分完成**表示实现存在但覆盖范围或验证不足；**未完成**表示没有足够证据。

| 原始要求 | 当前状态 | 直接证据 | 仍需推进 |
|---|---|---|---|
| Windows 10/11 图形程序，不因 PowerShell 语法闪退 | 已验证（x64） | WPF/.NET 8 自包含 EXE；最终 EXE 进程存活检查；全局 Dispatcher/AppDomain/Task 异常捕获 | 增加 ARM64 发布验证；32 位系统不在目标范围 |
| 选择任意 Minecraft 版本并自动分析 | 部分完成 | Mojang 全量版本清单；可编辑版本框；原版读取版本详情 | 各第三方发行版支持范围仍由上游决定；需在下载前显示更精确兼容结论 |
| 官方失败后权威/国内源回退 | 部分完成 | Mojang/Fabric/Forge 接 BMCLAPI；Java 接 Adoptium/Azul；真实损坏源切换测试 | Paper 当前只有官方源；Forge/Fabric 安装器内部依赖下载仍可能只走其内置源 |
| 完整报错、永不静默吞错 | 已验证 | 持久化 UTF-8 日志、完整异常窗口、HTTP/IO/进程退出码/控制台输出、三类顶层异常处理 | 继续扩大具体错误指纹和一键恢复操作 |
| 自动选择英文目录且不覆盖已有服务端 | 已验证 | 英文目录名、非法字符清理、非空同名目录加时间戳 | 失败安装目前需要会话恢复机制，避免每次新建目录 |
| EULA 自动同意、持续启动至成功 | 已验证（原版 1.7.10） | 真实 1.7.10 两次启动均到 `Done`；标准输入安全停服 | Paper/Fabric/Forge 需要真实矩阵验证 |
| 便携内存/Java配置和启动文件 | 已验证 | `server-pilot.json`、`Start-Server.cmd`、便携 Java 目录；交付后管理内存与版本感知中文 `server.properties` 面板 | — |
| 自动选择/下载最合适 Java | 已验证（代表版本） | Mojang `javaVersion` + 发行版规则；自动/仅已安装两种选择；1.17.1 缺 Java 16 时 Adoptium 不可用后切 Azul，下载/解压/探测并双启动成功；手动策略在下载前暂停 | Java 25 缺失下载仍只有规则和供应商能力证据 |
| 原版、模组服、插件服 | 已验证（代表版本） | Vanilla 1.7.10/1.17.1、Paper 1.21.11、Fabric 1.20、Forge 1.7.10/1.20 均完成真实安装或双启动 | 上游未发布的版本仍不能伪造支持；NeoForge/Quilt/Purpur 属扩展生态 |
| 安装测试模组/插件、启动、清理世界、纯净复测 | 已验证（Fabric/Forge 现代版） | Fabric 1.20 自动装 Fabric API+spark，首次实际加载 44 项，清理后只剩基础加载器；Forge 1.20 兼容性状态必须为 Passed；SHA-512 和限定路径清理 | 极老版本无安全测试组件时明确标记 LoaderOnly |
| 启动失败后分析并自动解决 | 已验证（可重复故障注入） | 启动前篡改 EULA + 抢占 25565 后连续修复并切 25566；假 Java 首次返回真实 JVM 内存保留错误后降至 512–2048 MB 并完成双启动；恢复报告持久化 | 未知第三方模组错误仍可能只能隔离并向用户报告 |
| 全自动模式一路同意，位置仍由用户选择 | 已实现 | Automatic 模式、位置选择、咖啡提示、无第三方 GUI 安装器 | 需要失败后自动续作 |
| 手动模式逐步阅读和确认 | 已验证 | 发行版/Java、EULA/安全配置、测试组件、交付四阶段确认；取消会话标记 Cancelled 且可续作 | — |
| 环境缺失、下载超时、源失效的网状纠错 | 已验证（代表路径） | Java双供应商、元数据重试、下载断点/超时/校验/换源、失败会话续作；Forge 1.20 首次超时后成功 | 第三方安装器内部的所有依赖源无法由外层完全控制 |
| 尽可能自动完成 GUI 安装器 | 已实现为更安全方案 | Java 使用免安装 ZIP；Forge 使用官方无界面 CLI | 不自动点击未知第三方安装器，这是刻意的可靠性设计 |
| 内网穿透与朋友联机指导 | 已实现，外网实测需用户网络 | 文件与程序内向导；局域网地址、监听状态、CGNAT/FRP、白名单；精确防火墙规则需确认与 UAC | 公网映射/第三方账号必须由用户提供路由器或隧道条件 |
| 自动化同时保证用户知情 | 部分完成 | 状态、进度、来源、警告、完整日志均可见 | 引导模式与高影响操作需要更细确认点 |

## 验收边界（不阻塞当前交付）

1. 验证 Windows 10 真机；当前直接运行证据来自 Windows 11，兼容目标由 `net8.0-windows` 与 API 使用范围支持。
2. NeoForge/Quilt/Purpur 可作为未来独立生态加入，但不能静默替换用户明确选择的 Forge/Fabric/Paper。
3. 公网连通性最终验证需要用户的真实路由器、公网 IPv4 或隧道账号条件。

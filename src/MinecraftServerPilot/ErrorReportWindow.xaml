<Window x:Class="MinecraftServerPilot.ErrorReportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="完整错误报告" Width="820" Height="560" MinWidth="650" MinHeight="420"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="这一步未能自动修复" FontSize="21" FontWeight="Bold" Foreground="#FF7B72" />
        <TextBlock Grid.Row="1" Text="没有静默失败：下面是完整异常链。可复制报告或打开持久化日志。"
                   Foreground="{StaticResource MutedBrush}" Margin="0,6,0,14" TextWrapping="Wrap" />
        <TextBox x:Name="ReportTextBox" Grid.Row="2" IsReadOnly="True" AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                 FontFamily="Cascadia Mono, Consolas" FontSize="11" TextWrapping="NoWrap" />
        <Grid Grid.Row="3" Margin="0,14,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="8" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="8" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="LogPathText" Foreground="{StaticResource MutedBrush}"
                       VerticalAlignment="Center" TextTrimming="CharacterEllipsis" Margin="0,0,15,0" />
            <Button Grid.Column="1" Content="复制报告" Click="CopyButton_OnClick" />
            <Button Grid.Column="3" Content="打开日志" Click="OpenLogButton_OnClick" />
            <Button Grid.Column="5" Content="我知道了" Click="CloseButton_OnClick"
                    Background="#238636" BorderBrush="#2EA043" />
        </Grid>
    </Grid>
</Window>
